Coloca aquí los recursos de empaquetado para `electron-builder`.

Archivos recomendados:

- `icon.ico` para Windows
- `icon.icns` para macOS
- `icon.png` para Linux

Si no existen, el build puede seguir funcionando, pero usará iconos por defecto
o tendrá resultados menos pulidos.
